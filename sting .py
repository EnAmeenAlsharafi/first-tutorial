"""
string can be as an object so we use with it many 
function
"""
title="welcome to the show boy"
print(title.upper())
print(title.count(title, 0, 5))
print(title.find('y'))
print(title.replace("l", "m")